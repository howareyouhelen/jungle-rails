<%= form_for(@product_spec) do |f| %>
  <% if @product_spec.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_spec.errors.count, "error") %> prohibited this product_spec from being saved:</h2>

      <ul>
      <% @product_spec.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
